# -*- mode: shell-script -*-

set -eu

print_and_exec() (
    echo "${PS4}${*}" 1>&2
    PS4=$(printf '%s' "${PS4}" | sed 's/\(.\) $/\1\1 /')
    PS4="${PS4}" "$@"
)

subcmd=$1

sudo='sudo'
case "${subcmd}" in
    search) sudo='' ;;
esac

print_and_exec $sudo apt "$@"
